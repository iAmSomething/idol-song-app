# App Routes

이 디렉터리는 Expo Router route 파일이 들어갈 위치다.

예정 구조:

```text
app/
  _layout.tsx
  (tabs)/
    _layout.tsx
    calendar.tsx
    radar.tsx
    search.tsx
  artists/[slug].tsx
  releases/[id].tsx
```

현재는 workspace bootstrap 단계라 route 파일은 아직 만들지 않는다.
