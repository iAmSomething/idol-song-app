# Mobile Web Handoff QA Matrix

- Generated at: 2026-03-08T09:54:42.649Z
- Evidence basis: production `web/src/lib/mobileWebHandoff.ts` code-path evaluation
- Scope: Android Chrome, iOS Safari, representative iOS in-app browser, installed/not-installed, canonical/search fallback, service-specific differences

## Service Behavior Summary

- `spotify`: Android Chrome and iOS Safari both attempt app-first for canonical and search links. Search mode uses spotify:search on app-open and open.spotify.com/search as the same-service web fallback.
- `youtube_music`: Android Chrome attempts app-first with an Android intent for canonical and search links. iOS contexts have no app-aware YouTube Music path in the current web implementation, so they remain web-only.
- `youtube_mv`: Android Chrome uses Android intent app-open for both watch URLs and search URLs. iOS Safari uses the YouTube custom scheme; in-app browsers should be treated as best-effort for external-app jump and must preserve the same-service web fallback.

## Browser Contexts

- `Android Chrome` (system browser / app-first intent or scheme path)
- `iOS Safari` (system browser / custom-scheme app-first path)
- `Instagram in-app browser (iOS)` (representative in-app browser / external-app jump may be suppressed)

## Detailed Matrix

| Browser | Service | Mode | App Installed | Code Path | QA Class | Observed Behavior | QA Expectation | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Android Chrome | spotify | canonical | yes | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | expected | Attempts Spotify app URI first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | spotify | canonical | no | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | spotify | search | yes | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | expected | Attempts Spotify app URI first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | spotify | search | no | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_music | canonical | yes | app_first_then_web_fallback<br/><small>intent://music.youtube.com/playlist?list=OLAK5uy_sample_release_object#Intent;scheme=https;package=com.google.android.apps.youtube.music;end</small> | expected | Attempts Android intent first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_music | canonical | no | app_first_then_web_fallback<br/><small>intent://music.youtube.com/playlist?list=OLAK5uy_sample_release_object#Intent;scheme=https;package=com.google.android.apps.youtube.music;end</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_music | search | yes | app_first_then_web_fallback<br/><small>intent://music.youtube.com/search?q=YENA%20LOVE%20CATCHER#Intent;scheme=https;package=com.google.android.apps.youtube.music;end</small> | expected | Attempts Android intent first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_music | search | no | app_first_then_web_fallback<br/><small>intent://music.youtube.com/search?q=YENA%20LOVE%20CATCHER#Intent;scheme=https;package=com.google.android.apps.youtube.music;end</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_mv | canonical | yes | app_first_then_web_fallback<br/><small>intent://www.youtube.com/watch?v=ABCDEFGHIJK#Intent;scheme=https;package=com.google.android.youtube;end</small> | expected | Attempts Android intent first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_mv | canonical | no | app_first_then_web_fallback<br/><small>intent://www.youtube.com/watch?v=ABCDEFGHIJK#Intent;scheme=https;package=com.google.android.youtube;end</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_mv | search | yes | app_first_then_web_fallback<br/><small>intent://www.youtube.com/results?search_query=YENA%20LOVE%20CATCHER%20official%20mv#Intent;scheme=https;package=com.google.android.youtube;end</small> | expected | Attempts Android intent first. If the external app opens, Chrome backgrounds; if Chrome stays visible, the same-service web URL loads after about 900ms. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| Android Chrome | youtube_mv | search | no | app_first_then_web_fallback<br/><small>intent://www.youtube.com/results?search_query=YENA%20LOVE%20CATCHER%20official%20mv#Intent;scheme=https;package=com.google.android.youtube;end</small> | expected | Stays in Chrome and loads the same-service web URL after about 900ms when the external target does not open. | app-first is expected; same-service web fallback must still be reachable | Chrome should preserve the same-service web fallback even when the app target does not open. |
| iOS Safari | spotify | canonical | yes | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | best_effort | Attempts Spotify app URI first. Safari may show an intermediate prompt; if the page stays visible, the same-service web URL loads after about 900ms. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | spotify | canonical | no | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | best_effort | Safari remains visible and the same-service web URL loads after about 900ms if the custom-scheme jump is unavailable. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | spotify | search | yes | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | best_effort | Attempts Spotify app URI first. Safari may show an intermediate prompt; if the page stays visible, the same-service web URL loads after about 900ms. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | spotify | search | no | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | best_effort | Safari remains visible and the same-service web URL loads after about 900ms if the custom-scheme jump is unavailable. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | youtube_music | canonical | yes | web_only | web_only | Opens the canonical youtube_music web URL immediately. Installed state does not change behavior for this service/context. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| iOS Safari | youtube_music | canonical | no | web_only | web_only | Opens the canonical youtube_music web URL immediately because no app-aware path exists here. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| iOS Safari | youtube_music | search | yes | web_only | web_only | Opens the search youtube_music web URL immediately. Installed state does not change behavior for this service/context. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| iOS Safari | youtube_music | search | no | web_only | web_only | Opens the search youtube_music web URL immediately because no app-aware path exists here. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| iOS Safari | youtube_mv | canonical | yes | app_first_then_web_fallback<br/><small>vnd.youtube://watch?v=ABCDEFGHIJK</small> | best_effort | Attempts YouTube iOS scheme first. Safari may show an intermediate prompt; if the page stays visible, the same-service web URL loads after about 900ms. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | youtube_mv | canonical | no | app_first_then_web_fallback<br/><small>vnd.youtube://watch?v=ABCDEFGHIJK</small> | best_effort | Safari remains visible and the same-service web URL loads after about 900ms if the custom-scheme jump is unavailable. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | youtube_mv | search | yes | app_first_then_web_fallback<br/><small>vnd.youtube://results?search_query=YENA%20LOVE%20CATCHER%20official%20mv</small> | best_effort | Attempts YouTube iOS scheme first. Safari may show an intermediate prompt; if the page stays visible, the same-service web URL loads after about 900ms. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| iOS Safari | youtube_mv | search | no | app_first_then_web_fallback<br/><small>vnd.youtube://results?search_query=YENA%20LOVE%20CATCHER%20official%20mv</small> | best_effort | Safari remains visible and the same-service web URL loads after about 900ms if the custom-scheme jump is unavailable. | app-first is best-effort; same-service web fallback must remain reachable | Safari custom-scheme behavior can vary by prompt timing; confirm the same-service web fallback still wins when the browser stays visible. |
| Instagram in-app browser (iOS) | spotify | canonical | yes | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | best_effort | Attempts Spotify app URI first inside the container. If the container keeps focus or suppresses the external-app jump, the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | spotify | canonical | no | app_first_then_web_fallback<br/><small>spotify:album:1234567890abcdefghijkl</small> | best_effort | No installed app target can be taken, so the in-app browser stays visible and the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | spotify | search | yes | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | best_effort | Attempts Spotify app URI first inside the container. If the container keeps focus or suppresses the external-app jump, the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | spotify | search | no | app_first_then_web_fallback<br/><small>spotify:search:YENA LOVE CATCHER</small> | best_effort | No installed app target can be taken, so the in-app browser stays visible and the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | youtube_music | canonical | yes | web_only | web_only | Opens the canonical youtube_music web URL immediately. Installed state does not change behavior for this service/context. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| Instagram in-app browser (iOS) | youtube_music | canonical | no | web_only | web_only | Opens the canonical youtube_music web URL immediately because no app-aware path exists here. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| Instagram in-app browser (iOS) | youtube_music | search | yes | web_only | web_only | Opens the search youtube_music web URL immediately. Installed state does not change behavior for this service/context. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| Instagram in-app browser (iOS) | youtube_music | search | no | web_only | web_only | Opens the search youtube_music web URL immediately because no app-aware path exists here. | web-only path should be treated as guaranteed for this browser context | Current web implementation has no iOS app-aware YouTube Music path, so this remains web-only even when the app is installed. |
| Instagram in-app browser (iOS) | youtube_mv | canonical | yes | app_first_then_web_fallback<br/><small>vnd.youtube://watch?v=ABCDEFGHIJK</small> | best_effort | Attempts YouTube iOS scheme first inside the container. If the container keeps focus or suppresses the external-app jump, the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | youtube_mv | canonical | no | app_first_then_web_fallback<br/><small>vnd.youtube://watch?v=ABCDEFGHIJK</small> | best_effort | No installed app target can be taken, so the in-app browser stays visible and the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | youtube_mv | search | yes | app_first_then_web_fallback<br/><small>vnd.youtube://results?search_query=YENA%20LOVE%20CATCHER%20official%20mv</small> | best_effort | Attempts YouTube iOS scheme first inside the container. If the container keeps focus or suppresses the external-app jump, the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |
| Instagram in-app browser (iOS) | youtube_mv | search | no | app_first_then_web_fallback<br/><small>vnd.youtube://results?search_query=YENA%20LOVE%20CATCHER%20official%20mv</small> | best_effort | No installed app target can be taken, so the in-app browser stays visible and the same-service web URL loads after about 900ms. | best-effort app-open only; web fallback must remain available | Use this row as the representative in-app browser scenario. External-app jump behavior can vary by container policy, so treat app-open as best-effort. |

## QA Interpretation

- `expected`: the current web implementation has an app-first path and the browser context should preserve a same-service web fallback when the app does not open.
- `best_effort`: the current web implementation tries app-first, but browser/container policy may suppress the external-app jump. QA should verify that the same-service web fallback still wins when the browser stays visible.
- `web_only`: the current web implementation has no app-aware branch for that service/context, so the same-service web URL is the intended behavior regardless of install state.
